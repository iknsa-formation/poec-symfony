{% extends 'ASTONAppBundle:Default:base.html.twig' %}

{% block body %}
  <h1>Liste des étudiants</h1>

  <table class="table">
    <thead>
      <tr>
        <th>Id</th>
        <th>Nom</th>
        <th>Prenom</th>
        <th>Email</th>
        <th>Titre</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      {% for etudiant in etudiants %}
        <tr>
          <td>
            <a href="{{ path('etudiants_show', { 'id': etudiant.id }) }}">{{ etudiant.id }}</a>
          </td>
          <td>{{ etudiant.nom }}</td>
          <td>{{ etudiant.prenom }}</td>
          <td>{{ etudiant.email }}</td>
          <td>{{ etudiant.titre }}</td>
          <td>
            <ul>
              <li>
                <a href="{{ path('etudiants_show', { 'id': etudiant.id }) }}">show</a>
              </li>
              <li>
                <a href="{{ path('etudiants_edit', { 'id': etudiant.id }) }}">edit</a>
              </li>
            </ul>
          </td>
        </tr>
      {% endfor %}
    </tbody>
  </table>
{% endblock %}
